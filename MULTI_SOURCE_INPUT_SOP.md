# StemKit 输入源多维拓展与端到端实操指南 (SOP)

本文档为 **StemKit**（多轨音频伴奏/分轨提取工作站）在全面扩展输入源（本地音频拖拽导入、移动端/微信录音流转、B站与外链直抓）后的完整技术规范与标准化操作程序（SOP）。

---

## 一、 核心价值与功能定位

1. **摆脱单一平台绑死**：此前仅能输入 YouTube 链接，在遭遇网络封锁、风控或版权下架时完全不可用。拓展后支持全格式本地音频（MP3、WAV、FLAC、M4A、AAC、OGG 等），成为断网依然可用的离线分轨工作站。
2. **打通真实生产力流**：乐手、制作人日常的大量音频存在于：微信群 Demo、手机现场排练录音、网盘下载的高保真无损 FLAC、B站/SoundCloud 小众音乐。多源输入打通了创作与练习的最后一公里。
3. **零等待急速处理（Zero-Download Fastpath）**：针对本地媒体，直接跳过网络下载环节，由内嵌 FFmpeg 毫秒级转换为 44.1kHz 标准工程流后直通 AI 分离引擎，处理速度较网络下载提升 300% 以上。

---

## 二、 能力边界与物理限制（Known Boundaries）

| 维度 | 优势与支持范围 | 能力边界与注意事项 |
| :--- | :--- | :--- |
| **音频采样质量** | 支持 CD 级（44.1k/48k）及以上 FLAC/WAV/320k MP3，分离精度最高。 | **低采样微信语音/电话录音（<16kHz）**：分离后会伴随金属电音和相位杂音，算法无法无中生有还原高频谐波。 |
| **视频同步与封面** | 本地视频文件（MP4/MKV）保留视频帧同步；纯音频自动降级为封面波形展示。 | 纯音频文件默认无动态画面，系统提取内嵌 ID3 封面或生成动态频响波形。 |
| **第三方平台抓取** | 支持 Bilibili 公开视频、SoundCloud、HTTP/HTTPS 直链。 | 需登录/大会员/付费/私有分享网盘（如百度网盘、阿里云盘提取码页面）无法直接爬取，需下载到本地后拖入。 |

---

## 三、 用户标准化实操程序（User SOP）

### 场景 1：电脑本地文件【拖拽与文件选择】导入
1. **拖拽导入**：在文件管理器（Finder / 资源管理器）中选中音频或视频（如 `MySolo.flac`），直接拖入 StemKit 窗口中央。
2. **界面响应**：软件全屏出现深紫色虚线框与提示：“**松开鼠标即可导入文件**”。
3. **松手即分离**：松开鼠标，系统自动读取元数据，无需网络下载，直接进入 `converting -> separating` 分离阶段。
4. **常规选择导入**：也可以点击搜索框右侧的 **【本地文件】** 按钮，调起系统原生文件选择窗口批量选取。

### 场景 2：微信聊天记录 / 手机录音机导入（移动端流转）
1. **获取源文件**：在手机端微信或录音机中长按音频文件；
2. **系统分享流转**：
   - **iOS 用户**：点击【分享】->【存储到“文件”】或通过 AirDrop 直接投送至 Mac；或者在同一局域网下通过浏览器投送；
   - **Android 用户**：点击【发送给好友/我的电脑】或通过蓝牙/数据线保存；
3. **一键载入**：投送至电脑后，直接将文件拖拽进入 StemKit 立即开始人声、鼓、贝斯、吉他提取。

### 场景 3：Bilibili 视频链接提取伴奏与分轨
1. **复制链接**：在 B 站复制任意视频链接（如 `https://www.bilibili.com/video/BV1xx411c7xx` 或手机端短链 `https://b23.tv/xxx`）；
2. **粘贴识别**：直接按 `Ctrl+V`（Mac 快捷键 `Cmd+V`）粘贴到 StemKit 搜索框中；
3. **分轨执行**：右侧按钮自动识别为 **【Split】**，点击后内置下载器自动注入 B 站防盗链头，边下边转码，完成 6 音轨分离。

---

## 四、 核心代码架构与技术变更明细

本次拓展主要修改与新增以下核心链路模块：

1. **`src/shared/url.ts` (多源解析引擎)**
   - 增加 `parseMediaSource(input)` 统一适配器；
   - 支持判断 `youtube`, `bilibili`, `soundcloud`, `direct_url`, `local_file`；
   - 针对非 YouTube 媒体，派生出具有防冲突特性的专属 ID 空间（`loc_*`, `bi_*`, `sc_*`, `url_*`）。

2. **`src/main/pipeline.ts` (预处理与分离流水线)**
   - 增加了 `parsedSource.isLocal` **本地直通快切分支**：直接校验本地文件真实性，省去全部网络请求和 `yt-dlp` 开销；
   - 在远程下载器中注入 Bilibili 专属请求头（`--referer: https://www.bilibili.com`、标准浏览器 User-Agent），彻底解决 403 跨域防盗链报错。

3. **`src/main/index.ts` & `src/preload/index.ts` (IPC 桥接层)**
   - 注册 `dialog:open-audio-file` 原生文件选择事件；
   - 扩展 `StemKitApi` 开放 `openFileDialog()`。

4. **`src/renderer/src/components/Home.tsx` (用户交互层)**
   - 新增全屏 `dragOver`、`dragLeave`、`drop` 拖拽监听与高亮波纹蒙层；
   - 搜索栏新增 `[ 📁 本地文件 ]` 交互按钮；
   - 优化提示语与输入兼容性。

5. **`src/renderer/src/components/Player.tsx` (多源播放器)**
   - 识别 `bi_*` 视频提供一键直达 Bilibili 原片按钮；
   - 识别 `loc_*` 本地媒体展示高保真“本地媒体”标签，避免无效的 YouTube 页面跳转。
